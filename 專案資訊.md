# 專案資訊 — DinoDashboard

| 欄位 | 內容 |
|---|---|
| 本地資料夾 | `C:\Users\AG_Di\Desktop\automation\Claude_code\automatic_evolution\DinoDashboard` |
| 本地輸出位置 | 無（網頁應用；資料存在 Supabase Postgres＋Storage，截圖桶 `screenshots`） |
| 安裝 | 雙擊 `C:\Users\AG_Di\Desktop\automation\Claude_code\automatic_evolution\DinoDashboard\安裝.bat`（建 .venv、裝 requirements.txt；需 Python 3.13） |
| 啟動 | 雙擊 `C:\Users\AG_Di\Desktop\automation\Claude_code\automatic_evolution\DinoDashboard\啟動.bat`（啟動 Flask 並自動開瀏覽器） |
| 本地 | http://localhost:5050 |
| GitHub | https://github.com/dino-q/dinodashboard |
| 線上 | https://dinodashboard.onrender.com |

備註：

- `.env` 必填 `SUPABASE_URL` / `SUPABASE_SECRET_KEY` / `FLASK_SECRET_KEY`（已在本地資料夾內，不進 git）。
- 線上部署在 Render（`render.yaml` Blueprint），push 到 GitHub main 會自動部署；免費方案 15 分鐘無流量會 sleep，cron-job.org 每 5 分鐘打 `/ping` 保活。
